# Packer's ansible provisioner manages connection details (SSH user, key,
# host) dynamically. This inventory just declares the symbolic group the
# bake playbook targets.

[default]
default ansible_connection=ssh
