---
- name: repmgr-setup
  hosts: all
  become: true
  become_user: root
  tasks:
    - name: Ping all nodes
      ansible.builtin.ping:
  roles:
    # - role: postgresql/dependencies
    - role: postgresql/repmgr-configuration
