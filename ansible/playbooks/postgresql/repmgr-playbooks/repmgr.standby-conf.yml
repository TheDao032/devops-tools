---
- name: repmgr-standby-config
  hosts: slave
  become: true
  become_user: root
  tasks:
    - name: Ping all nodes
      ansible.builtin.ping:
  roles:
    - role: ../../roles/postgresql/repmgr/repmgr-standby-conf
