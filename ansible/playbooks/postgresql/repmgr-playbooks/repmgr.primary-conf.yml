---
- name: repmgr-primary-config
  hosts: repmgr
  become: true
  become_user: root
  tasks:
    - name: Ping all nodes
      ansible.builtin.ping:
  roles:
    - role: ../../roles/postgresql/repmgr/repmgr-primary-conf
