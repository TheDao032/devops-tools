---
- name: dependencies-setup
  hosts: vault-server
  become: true
  become_user: root
  tasks:
    - name: Ping all nodes
      ansible.builtin.ping:
  roles:
    - role: ../../roles/docker/dependencies
