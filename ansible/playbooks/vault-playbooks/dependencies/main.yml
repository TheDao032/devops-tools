---
- name: dependencies-setup
  hosts: server
  become: true
  become_user: root
  tasks:
    - name: Ping all nodes
      ansible.builtin.ping:
  roles:
    - role: ../../roles/vault/dependencies
    - role: ../../roles/vault/impl-config
