---
- name: repmgr-coordinator-conf
  hosts: coordinator
  become: true
  become_user: root
  tasks:
    - name: Ping all nodes
      ansible.builtin.ping:
  roles:
    - role: ../../roles/postgresql/citus/coordinator-conn-worker
