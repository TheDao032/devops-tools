---
# vars file for vault/dependencies

redhat:
  common_packages:
    - yum-utils
    - curl
    - jq
  vault_packages:
    - vault

debian:
  common_packages:
    - curl
    - gpg
    - wget
    - jq
  vault_packages:
    - vault
