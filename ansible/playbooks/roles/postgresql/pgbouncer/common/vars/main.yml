---
# vars file for common
common_packages:
  - pgbouncer
pgbouncer_scripts_path: /usr/bin
pgbouncer_scripts:
  - pgbouner-ini-conf.sh
auth_user: "postgres"
db_name: "postgres"
db_host: "{{ psql_coordinator_ip }}"
db_port: "{{ psql_coordinator_port }}"
