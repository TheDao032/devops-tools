---
galaxy_info:
  role_name: citus_docker
  author: devops-architect
  description: >-
    Alpine/aarch64-native Citus dev cluster (coordinator + workers + pgbouncer) run as
    Docker containers on a single node (k3s-lb). No repmgr. Chosen because the citusdata
    apt repo and the official citusdata/citus image are amd64-only.
  license: MIT
  min_ansible_version: "2.14"
dependencies: []
