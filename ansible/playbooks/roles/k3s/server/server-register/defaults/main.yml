---
# defaults file for server-register
api_port: 6443  # noqa var-naming[no-role-prefix]
api_endpoint: "{{ hostvars[groups['server'][0]]['ansible_host'] | default(groups['server'][0]) }}"  # noqa var-naming[no-role-prefix]
