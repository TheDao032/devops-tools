# Renesas / dev / k3s / proxmox — small VM dev cluster.

all:
  children:
    server:
      hosts:
        renesas-k3s-dev-pve-server-1:
          ansible_host: 10.42.40.11
    agent:
      hosts:
        renesas-k3s-dev-pve-agent-[1:2]:
          ansible_host: "10.42.40.{{ 20 + (inventory_hostname | regex_search('[0-9]+$') | int) }}"
  vars:
    proxmox_api_host: pve.renesas.internal     # TODO
    proxmox_node: pve02                         # TODO: dev node
    proxmox_storage: local-zfs                  # TODO
