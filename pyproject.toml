[project]
name = "devops-tools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "hvac>=2.3.0",
    "python-dotenv>=1.0.1",
    "pyyaml>=6.0.2",
]
